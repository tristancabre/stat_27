---
title: "analyses bivariées"
format: html
editor: visual
---

# Analyses bivariées

## Packages nécéssaires

```{r}
install.packages("FactoMineR")
install.packages("factoextra")  # pour les graphiques
library(FactoMineR)
library(factoextra)
```

```{r}
summary(pisa3$READ)

summary(pisa3$MATH) 
```

```{r}
hist(pisa3$READ)
```

```{r}
hist(pisa3$MATH)
```
